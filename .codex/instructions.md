# Project Instructions

After each completed feature-sized change in this repository:

- run a relevant validation check when practical,
- commit the change with a short imperative message,
- push the commit to `origin/master`,
- confirm the worktree is clean before starting the next feature.

Do not batch unrelated feature changes into one commit.
